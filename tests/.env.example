# Copy to tests/.env.local (gitignored) and fill in. NEVER commit real values.
# A driver/transporter UAT account (the transporter portal expects one).

# Optional: override the app URL the tests hit (default: the local dev server).
# E2E_BASE_URL=http://localhost:5173

E2E_KEYCLOAK_URL=https://auth-rtms.ayinza.dev
E2E_REALM=ilgars
E2E_CLIENT_ID=ilgars-ui
E2E_USERNAME=
E2E_PASSWORD=
